---
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.crypto
    version: ">=2.15.0"
  - name: community.docker
    version: ">=3.8.0"
  - name: netbox.netbox
    version: ">=3.18.0"
  - name: community.sops
    version: ">=1.6.0"
  # NOTE: elastic.elasticsearch retirée — la collection était pinned à
  # >=0.12.0 (pre-release 1.0.0-rc) et nous n'utilisons aucun de ses modules
  # dans nos rôles. Si un usage devient nécessaire (FW3), réactiver ici en
  # alignant la version sur une release stable.

---
- name: Bastion placeholder playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Show bastion placeholder message
      ansible.builtin.debug:
        msg: "Placeholder playbook for bastion deployment"

---
- name: Site B placeholder playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Show Site B placeholder message
      ansible.builtin.debug:
        msg: "Placeholder playbook for Site B configuration"

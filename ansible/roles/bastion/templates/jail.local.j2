# Fail2ban jail — bastion (plus agressif que le baseline)

[sshd]
enabled  = true
port     = {{ bastion_wan_port | default(2222) }}
filter   = sshd
bantime  = 86400
findtime = 600
maxretry = 3
backend  = systemd

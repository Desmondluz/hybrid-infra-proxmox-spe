# Input Syslog (pfSense envoie en RFC5424 UDP/514)
input {
  udp {
    port => 5514
    type => "syslog"
    codec => plain { charset => "UTF-8" }
  }
  tcp {
    port => 5514
    type => "syslog"
    codec => plain { charset => "UTF-8" }
  }
}
